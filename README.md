# bball-score-react

A basic build of bball-score using React.

This is intended to connect to the [bball-score-server](/ober9k/bball-score-server) repository for providing an API to render this application.